// This is exported here so it can be imported directly in other configs
/* eslint-disable @typescript-eslint/no-var-requires */
const baseStyleConfig = require('@acolorbright/eslint-config/rules/style');

module.exports = baseStyleConfig;
